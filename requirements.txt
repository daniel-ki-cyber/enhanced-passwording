# Core dependencies
# SECURITY: Pin exact versions to prevent supply chain attacks
# Update versions intentionally after security review
cryptography==45.0.4

# API framework
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.5

# Security
slowapi==0.1.9  # Rate limiting for brute force protection
PyJWT==2.10.1   # JWT token authentication

# Testing (pinned for reproducibility)
pytest==9.0.2
pytest-cov==7.0.0
httpx==0.28.1  # For testing FastAPI
